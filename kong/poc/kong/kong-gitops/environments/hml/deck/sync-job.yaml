apiVersion: batch/v1
kind: Job
metadata:
  name: kong-deck-sync
  namespace: kong
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
spec:
  template:
    spec:
      containers:
        - name: deck
          image: kong/deck:v1.56.0
          command:
            - deck
          args:
            - gateway
            - sync
            - /config/kong.yaml
            - --kong-addr
            # Usar nome curto do service (sem FQDN) — resolve bug de DNS
            # da musl libc (Alpine) com ndots:5 no Kubernetes.
            - http://kong-cp-kong-admin:8001
          volumeMounts:
            - name: kong-config
              mountPath: /config
              readOnly: true
      volumes:
        - name: kong-config
          configMap:
            name: kong-deck-state
      restartPolicy: Never
  backoffLimit: 2
